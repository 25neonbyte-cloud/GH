import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useAuth } from '../context/AuthContext'; import { errMsg } from '../services/api';
export default function Login(){const {login}=useAuth();const nav=useNavigate();const [form,setForm]=useState({username:'admin',password:'Admin123!'});const [error,setError]=useState('');const submit=async e=>{e.preventDefault();try{setError('');await login(form.username,form.password);nav('/');}catch(e){setError(errMsg(e));}};return <div className="min-h-screen grid place-items-center bg-slate-100 p-4"><form onSubmit={submit} className="card p-6 w-full max-w-sm"><h1 className="text-2xl font-bold">Hospital PRJT</h1><p className="text-slate-500 mb-5">Acesso ao sistema de gestão</p>{error&&<div className="bg-red-50 text-red-700 p-3 rounded mb-3">{error}</div>}<label className="label">Usuário</label><input className="input mb-3" value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/><label className="label">Senha</label><input type="password" className="input mb-5" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn btn-primary w-full">Entrar</button></form></div>}
