import { useState } from 'react';import { api,errMsg } from '../services/api';import { ErrorBox,PageTitle } from '../components/Common';
export default function Dados(){const [file,setFile]=useState(null),[preview,setPreview]=useState(null),[result,setResult]=useState(null),[error,setError]=useState('');const send=async mode=>{if(!file)return;const fd=new FormData();fd.append('file',file);try{setError('');const {data}=await api.post(`/sync/${mode}`,fd,{headers:{'Content-Type':'multipart/form-data'}});mode==='preview'?setPreview(data):setResult(data)}catch(e){setError(errMsg(e));if(e.response?.data?.details)setPreview({valid:false,errors:e.response.data.details})}};const exportar=async()=>{const r=await api.get('/sync/exportar',{responseType:'blob'});const u=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=u;a.download='hospital-export.xlsx';a.click();URL.revokeObjectURL(u)};return <><PageTitle title="Importar / Exportar" subtitle="Excel é migração, contingência e saída; não é fonte operacional"/><ErrorBox error={error}/><div className="grid lg:grid-cols-2 gap-4"><div className="card p-5"><h2 className="font-bold mb-3">Importação controlada</h2><input type="file" accept=".xlsx,.xls" onChange={e=>{setFile(e.target.files[0]);setPreview(null);setResult(null)}}/><div className="flex gap-2 mt-4"><button className="btn btn-secondary" disabled={!file} onClick={()=>send('preview')}>Validar</button><button className="btn btn-primary" disabled={!file||preview?.valid!==true} onClick={()=>send('importar')}>Importar</button></div>{preview&&<div className="mt-4 text-sm"><b>{preview.valid?'Planilha válida':'Erros encontrados'}</b>{preview.errors?.map((x,i)=><div key={i} className="text-red-700">{x.aba} linha {x.linha}: {x.erro}</div>)}</div>}{result&&<pre className="mt-4 bg-slate-50 p-3 rounded text-xs overflow-auto">{JSON.stringify(result,null,2)}</pre>}</div><div className="card p-5"><h2 className="font-bold mb-3">Exportação</h2><p className="text-sm text-slate-500 mb-4">Gera Excel com pacientes, leitos e internações.</p><button className="btn btn-primary" onClick={exportar}>Exportar todos os dados</button></div></div></>}
