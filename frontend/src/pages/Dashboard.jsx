import { useEffect,useState } from 'react'; import { api,errMsg } from '../services/api'; import { ErrorBox,PageTitle } from '../components/Common';
export default function Dashboard(){const [d,setD]=useState(null);const [hist,setHist]=useState([]);const [error,setError]=useState('');const load=async()=>{try{const [a,b]=await Promise.all([api.get('/dashboard/indicadores'),api.get('/dashboard/ocupacao-7dias')]);setD(a.data);setHist(b.data.data);setError('');}catch(e){setError(errMsg(e));}};useEffect(()=>{load();const t=setInterval(load,300000);return()=>clearInterval(t)},[]);const cards=d?[['Taxa de ocupação',`${d.taxaOcupacao}%`],['Leitos livres',d.leitosLivres],['Leitos ocupados',d.leitosOcupados],['Internações ativas',d.internacoesAtivas],['Média LOS',`${d.mediaLOS} dias`],['Altas hoje',d.altasHoje]]:[];return <><PageTitle title="Dashboard" subtitle="Visão operacional calculada automaticamente"/><ErrorBox error={error}/><div className="grid sm:grid-cols-2 xl:grid-cols-3 gap-4">{cards.map(([k,v])=><div className="card p-5" key={k}><div className="text-slate-500 text-sm">{k}</div><div className="text-3xl font-bold mt-2">{v}</div></div>)}</div><div className="card p-5 mt-5"><h2 className="font-bold mb-4">Ocupação — 7 dias</h2><div className="space-y-3">{hist.map(x=><div key={x.data} className="grid grid-cols-[100px_1fr_60px] items-center gap-3 text-sm"><span>{new Date(x.data+'T12:00').toLocaleDateString('pt-BR',{day:'2-digit',month:'2-digit'})}</span><div className="h-3 bg-slate-100 rounded-full overflow-hidden"><div className="h-full bg-blue-600" style={{width:`${Math.min(x.taxa,100)}%`}}/></div><b>{x.taxa}%</b></div>)}</div></div></>}
