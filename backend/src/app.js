import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import rateLimit from 'express-rate-limit';
import auth from './routes/auth.js'; import usuarios from './routes/usuarios.js'; import pacientes from './routes/pacientes.js'; import leitos from './routes/leitos.js'; import internacoes from './routes/internacoes.js'; import dashboard from './routes/dashboard.js'; import tv from './routes/tv.js'; import profissionais from './routes/profissionais.js'; import escala from './routes/escala.js'; import prontuario from './routes/prontuario.js'; import departamentos from './routes/departamentos.js'; import sync from './routes/sync.js'; import backups from './routes/backups.js'; import { notFound,errorHandler } from './middleware/error.js';
export const app=express();
app.use(helmet());app.use(cors({origin:(process.env.FRONTEND_URL||'http://localhost:5173').split(','),credentials:true}));app.use(express.json({limit:'2mb'}));app.use(rateLimit({windowMs:15*60*1000,limit:1000,standardHeaders:true,legacyHeaders:false}));
app.get('/health',(req,res)=>res.json({status:'OK',timestamp:new Date()}));
app.use('/api/auth',auth);app.use('/api/usuarios',usuarios);app.use('/api/pacientes',pacientes);app.use('/api/leitos',leitos);app.use('/api/internacoes',internacoes);app.use('/api/dashboard',dashboard);app.use('/api/tv',tv);app.use('/api/profissionais',profissionais);app.use('/api/escala',escala);app.use('/api/prontuario',prontuario);app.use('/api/departamentos',departamentos);app.use('/api/sync',sync);app.use('/api/backups',backups);app.use(notFound);app.use(errorHandler);
